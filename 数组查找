在数组a={23,56,11,87,91,35,41,44,60,6} 中查找一个数x，x的值由用户输入，若数组存在该数，则输出该数在数组中的位置，否则提示不存在。


#include <stdio.h>
int main()
{
	int a[10]={23,56,11,87,91,35,41,44,60,6},x,i;
	scanf("%d",&x);
	for(i=0;i<=9;i++)
	{
		if(x==a[i])
		{
			printf("%d position is %d",x,i);break;
		}
	}
	if(i==10) printf("%d could not be found",x);
	return 0;
}
